apiVersion: batch/v1
kind: Job
metadata:
  name: pi
spec:
  template:
    spec:
      containers:
        - name: pi
          image: avnikulin/otus-hw1-architect:1.0.0
          command: ["java", "-version", "&&" ,"ls"]
      restartPolicy: Never
