apiVersion: v1
kind: Service
metadata:
  name: avn-otus-hw1-architect-service
spec:
  selector:
    app: avn-otus-hw1-architect-app
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: NodePort